import { defineTheme } from './defineTheme';

/**
 * The default theme — a native port of @astryxdesign/theme-neutral:
 * muted, minimal, system fonts.
 */
export const neutralTheme = defineTheme({ name: 'neutral' });
